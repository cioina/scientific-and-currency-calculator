Constants
{
FROM = _from //Internal constant;
TO = _to //Internal constant;
STEP = _step //Internal constant;
COLWIDTH = _colwidth //Internal constant;
DETTOLERANCE = _dettolerance //Internal constant;
INTEGRTOLERANCE = _integrtolerance //Internal constant;
}

Function variable
{
x
}

User definitions
{
d=654.38,
f(x,y,z)~exp(x)^2+sqrt(sin(y))+z^2,
Fact(n)~if(eq(n,0),1,n*Fact(n-1))
}

Mathematical expressions
{
sin(pi), COS(pi),
e,
today,
now,
DAYSPAN(1.31.2008, today),
CONJUGATE(complex(-pi,-e)),
real2curr(f(pi,e,d)),
max(1,2,3,4,5,6,7,8,9),
Fact(16),
not(and(True,false,or(false,true,false)))
}
